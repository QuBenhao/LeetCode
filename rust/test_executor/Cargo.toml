[package]
name = "test_executor"
version = "0.1.0"
edition = "2021"
rust-version = "1.79.0"
authors = ["benhao"]
description = "LeetCode Rust Test Main"
readme = "../README.md"

[features]
timer = []
test_case = []

[dependencies]
serde_json = "1.0"
problems = { path = "../../problems", features = ["solution"] }
